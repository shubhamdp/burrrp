# Jekyll configuration
markdown: kramdown
highlighter: rouge
paginate: 100 # it will paginate after this number
language: 'en-uk'
words_per_minute: 200

# edit baseurl to simply '/' if using as your personal page (instead of a project page)
baseurl: /burrrp/

# Website info
title: Nomad
description: travel, tech, experience dairies
cover: assets/images/blog-cover.svg
logo: assets/images/logo-unk.png
logo_dark: assets/images/logo-unk.png
favicon: assets/images/favicon.svg
CNAME: #add cname for website to be created

# Social info
# navigation: True # show the navbar links
# subscribers: True # you need to connect an external service for this to work,
# if 'True' the submit button is disabled for now, but you can change that
# by editing `_includes/subscribe-form.html`

# Google Analytics
google_analytics: G-61Z4NMMPMQ # replace with your GA tracking identifier

# Permalinks
permalink: /:title

# Authors' info in `_data/authors.yml`
# Tags' info in `_data/tags_info.yml`

# gems and other configs
plugins_dir: [_plugins]
plugins: [jekyll-paginate, jekyll-feed]

feed:
  path: feed.xml

exclude:
  - assets/css
  - node_modules
  - vendor
  - .travis.yml
  - Gemfile
  - Gemfile.lock
  - GHOST.txt
  - gulpfile.js
  - LICENSE
  - package.json
  - package-lock.json
  - Rakefile
  - README.md
  - script.py
  - changelog.md
  - "*.Rmd"
  - .git*
